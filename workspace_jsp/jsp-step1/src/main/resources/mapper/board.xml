<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.mapper.BoardMapper">
	<select id="boardList" parameterType="java.util.Map" resultType="java.util.Map"> select b_no,
		b_title, b_writer, b_content from react_board </select>
	<select id="curent" parameterType="java.util.HashMap" resultType="java.lang.String"> select
		to_char(sysdate, 'YYYY-MM-DD HH:MI:SS') from dual </select>
</mapper>